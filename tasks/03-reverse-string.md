# Reverse String

Write `reverse_string(s: str) -> str` that returns the reverse of `s`.

## Examples

```
reverse_string("hello") == "olleh"
reverse_string("") == ""
reverse_string("a") == "a"
```

## Constraints

- Handle empty and single-char strings
- Unicode is fine (Python strings are already unicode)